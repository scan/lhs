bundle:
  exclusive-executor-resource: bundle
  timeout: 10 Minutes
  start-when:
    - script: ruby
  body: |
    sed 's/^source.*/source "http\:\/\/52.29.7.59:9292"/g' Gemfile > Gemfile.tmp ; mv Gemfile.tmp Gemfile
    echo $(rbenv which bundle)
    $(rbenv which bundle)
